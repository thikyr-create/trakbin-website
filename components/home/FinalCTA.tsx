import { CTABand } from "@/components/shared/CTABand";

export function FinalCTA() {
  return (
    <CTABand title="Let's build better waste operations."
      lead="Interested in deploying Trakbin? Start with a pilot, we deploy with you on a real zone, measure everything, and build from real operations." />
  );
}